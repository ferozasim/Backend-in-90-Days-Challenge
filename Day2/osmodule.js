const os = require('os');

console.log(os.platform());  // for getting the platform of the operating system 
console.log(os.arch());      // for getting the architecture of the operating system
console.log(os.cpus());      // for getting information about the CPU cores

console.log(os.hostname());   // for getting the amount of free memory in bytes
